# Test starter repository

This repository was prepared from the `test-starter` autorepo fixture pack.

Use it to verify that collection loading, pack selection, operation preview, and warmup rendering are wired correctly.

<!-- autorepo:pack=test-starter;id=file.readme -->
