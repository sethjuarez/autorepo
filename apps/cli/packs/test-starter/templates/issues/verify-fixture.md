Use this issue to confirm the small fixture pack can be inspected and rendered safely.

Expected checks:

1. The pack appears in the collection.
2. The operation preview shows four write operations.
3. The local warmups tab shows two warmup items.

<!-- autorepo:pack=test-starter;id=issue.verify_fixture -->
